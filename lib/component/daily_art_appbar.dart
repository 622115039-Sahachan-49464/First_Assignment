import 'package:flutter/material.dart';

class DailyArtAppBar extends StatelessWidget {
  const DailyArtAppBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
