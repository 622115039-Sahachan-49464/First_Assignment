import 'package:flutter/material.dart';

class DailyArtArticle extends StatelessWidget {
  const DailyArtArticle({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
